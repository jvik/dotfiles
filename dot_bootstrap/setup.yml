---
- name: System setup and package installation
  hosts: localhost
  become: true
  gather_facts: true
  vars_files:
    - ~/.bootstrap/vars.yml
  roles:
    - repositories
    - packages
    - shell
    - dev_tools
    - system
