---
- name: Install sway packages
  ansible.builtin.package:
    name:
      - sway
      - wofi
      - SwayNotificationCenter
      - waybar
      - wdisplays
      - lxpolkit
    state: present
  when: window_manager == "sway"
