---
- name: Check if HomeBrew is installed
  become: true
  become_user: "{{ user }}"
  ansible.builtin.stat:
    path: /home/linuxbrew/.linuxbrew/bin/brew
  register: homebrew_brew

- name: Download & Install HomeBrew
  become: true
  become_user: "{{ user }}"
  when: not homebrew_brew.stat.exists
  block:
    - name: Create tmp dir
      ansible.builtin.file:
        path: '/home/{{ user }}/temp'
        state: directory
        mode: '0755'

    - name: Download Installer Script
      ansible.builtin.get_url:
        url: https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh
        dest: /home/{{ user }}/temp/brew_install.sh
        mode: '775'

    - name: Run Installer Script
      ansible.builtin.command: /home/{{ user }}/temp/brew_install.sh
      environment:
        NONINTERACTIVE: '1'
      changed_when: true

- name: Fix Homebrew directory ownership
  ansible.builtin.command: chown -R {{ user }}:{{ user }} /home/linuxbrew/.linuxbrew
  become: true
  changed_when: true
  failed_when: false

- name: Update Homebrew
  ansible.builtin.command: /home/linuxbrew/.linuxbrew/bin/brew update
  become: true
  become_user: "{{ user }}"
  changed_when: false
  environment:
    PATH: "/home/linuxbrew/.linuxbrew/bin:{{ ansible_env.PATH }}"

- name: Set LinuxBrew Executable Path
  ansible.builtin.set_fact:
    homebrew_executable: /home/linuxbrew/.linuxbrew/bin/brew
    homebrew_path: /home/linuxbrew/.linuxbrew/bin
