---
- name: Install packages
  ansible.builtin.package:
    name:
      - fd-find
      - tree
      - flatpak
      - ansible-lint
      - htop
      - blueman
      - unrar
      - zsh
      - git
      - neovim
      - code
      - playerctl
      - copyq
      - wl-mirror
      - firefox
      - solaar
      - jotta-cli
    state: present
    update_cache: true
